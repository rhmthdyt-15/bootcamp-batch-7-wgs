<div class="container">
    <div class="row">
        <div class="col-md-6 mx-auto">
            <div class="card mt-4">
                <div class="card-body">
                    <h2 class="card-title mb-4">Tambah Data</h2>

                    <!-- Menampilkan pesan kesalahan menggunakan Bootstrap alert -->
                    <% if (typeof errors !== 'undefined' && errors.length > 0) { %>
                    <div class="alert alert-danger">
                        <strong>Kesalahan Validasi:</strong>
                        <ul>
                            <% errors.forEach(error => { %>
                            <li><%= error.msg %></li>
                            <% }); %>
                        </ul>
                    </div>
                    <% } %>

                    <form method="POST" action="/contact/tambah">
                        <div class="form-group">
                            <label for="name">Nama</label>
                            <input type="text" class="form-control" id="name" name="name" value="<%= formData?.name || '' %>">
                            <!-- Menampilkan pesan kesalahan khusus untuk nama -->
                            <% if (typeof nameError !== 'undefined' && nameError !== null) { %>
                            <small class="text-danger"><%= nameError %></small>
                            <% } %>
                        </div>
                        <div class="form-group">
                            <label for="phone">Nomor Telepon</label>
                            <input type="tel" class="form-control" id="phone" name="phone" value="<%= formData?.phone || '' %>">
                            <!-- Menampilkan pesan kesalahan khusus untuk nomor telepon -->
                            <% if (typeof phoneError !== 'undefined' && phoneError !== null) { %>
                            <small class="text-danger"><%= phoneError %></small>
                            <% } %>
                        </div>
                        <div class="form-group">
                            <label for="contactEmail">Email</label>
                            <input type="email" class="form-control" id="contactEmail" name="email" value="<%= formData?.email || '' %>">
                            <!-- Menampilkan pesan kesalahan khusus untuk email -->
                            <% if (typeof emailError !== 'undefined' && emailError !== null) { %>
                            <small class="text-danger"><%= emailError %></small>
                            <% } %>
                        </div>
                        <button type="submit" class="btn btn-primary">Simpan</button>
                        <a href="/contact" class="btn btn-warning text-white">Kembali</a>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
